{
  "name": "kaburan",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "@hotwired/turbo-rails": "^8.0.0",
    "@hotwired/stimulus-loading": "^2.0.0"
  },
  "scripts": {
    "build": "bun build app/javascript/application.js --outdir app/assets/builds --target=browser --format=esm"
  }
}

